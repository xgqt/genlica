#!/bin/sh


# Update genlica


set -e
export PATH


main() {

	git reset --hard
	git pull

	stow -D -v portage -t "${EPREFIX}/etc/portage"
	stow -v portage -t "${EPREFIX}/etc/portage"
}


if [ "$(whoami)" != root ] && [ -z "${EPREFIX}" ]
then
	sudo "$0"
else
	main
fi
